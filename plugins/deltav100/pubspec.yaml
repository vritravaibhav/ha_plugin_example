name: deltav100
description: "Delta V100 hardware-accelerated drawing plugin (Nomivision WhiteBoardSpeedup)."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.hardwareAccelerated.deltav100
        pluginClass: Deltav100Plugin
